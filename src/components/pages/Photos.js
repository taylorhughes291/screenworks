/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react"

const Photos = () => {
    return (
        <div
            css={css`
                font-weight: 800;
            `}
        >Photos</div>
    )
};

export default Photos;
